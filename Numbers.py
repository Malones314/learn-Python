#Number
#有int、long、float、complex基本数字类型
#加（+）减（-）乘（*）除（/）幂运算（**）
#其中long类型只存在于python2代版本中，在2.2及其后面的版本int溢出后自动变为long类型，python3中删除long类型，用int替代。
#可以使用a+bj表示复数，也可使用complex( a, b)表示

#################################################################################################################
'''
    a = 1 + 2j
    b = 2 + 3j
    c = a + b
    print( c )    #(3+5j)
'''
#################################################################################################################

#python允许同时为同类型、不同类型的变量同时赋值，方法如下：

#################################################################################################################
'''
    a, b, s = 1, 2, "python"
    a = b = c = 1
'''
#################################################################################################################

#math中的函数
#在使用前，要先导入，import math
#很多函数和其他高级语言中的类似。

    #modf(number)函数, 第一为小数部分，第二位整数部分，整数部分使用浮点数表示
    #############################################################################################################
    '''
        import math
        a, b = math.modf(789.123456)
        print(a, b)             #0.12345600000003287 789.0
    
    '''
    #############################################################################################################
